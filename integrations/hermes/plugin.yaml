name: hermes-mnemosyne
version: 3.1.0
description: "Native local memory for Hermes — SQLite with vector search, FTS5 hybrid ranking, episodic consolidation, and temporal triples. Zero cloud. Zero latency. Pip-installable plugin via `pip install mnemosyne-hermes`."
author: Abdias J
pip_dependencies:
  - mnemosyne-memory>=3.1
